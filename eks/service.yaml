apiVersion: v1
kind: Service
metadata:
  name: banking-system-api-service
spec:
  selector:
    app: banking-system-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer